{
  "name": "jest-test",
  "version": "0.1.0",
  "engines": {
    "node": "^6.12.2"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest --env=node"
  },
  "dependencies": {
    "passport": "^0.4.0",
    "passport-anonymous": "^1.0.1"
  },
  "devDependencies": {
    "jest": "^22.1.4"
  }
}
